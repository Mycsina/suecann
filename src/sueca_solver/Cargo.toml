[package]
name = "sueca_solver"
version = "0.1.0"
edition = "2024"

[lib]
name = "sueca_solver"
crate-type = ["rlib"]

[dependencies]
rayon = "1.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
proptest = "1.4"

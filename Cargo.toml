[workspace]
members = [
    "src/sueca_solver",
    "src/sueca_wann",
    "src/sueca_wasm"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"

# Temporary profiling profile
[profile.perf]
inherits = "release"
debug = 1
